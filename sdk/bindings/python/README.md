# GenieX-Bridge Python Binding

> **Run next-generation LLMs and VLMs locally, natively, and at top speed on Qualcomm platforms.**

This binding is the Python SDK for GenieX-Bridge.

The current runtime package name remains `geniex` until the package rename is implemented in code and distribution metadata.

---

## Features

- 🚀 **NPU‑First Performance** — Accelerated AI with automatic device selection and backend optimizations.
- 🔮 **Day‑0 Architecture Support** — Run new LLMs, VLMs, ASR, and CV models immediately (GGUF).
- 🧩 **True Multimodality** — Seamless pipelines for text, vision, and audio.
- 🤖 **OpenAI-Compatible Server** — Supports serving, chat, and function calling.
- 🏆 **Qualcomm Platform Focus** — Windows ARM64, Linux ARM64, and Android (Hexagon/QNN).

---

## What is it?

The Python binding offers a clean API over the GenieX-Bridge runtime. Build modern AI applications that run fully **on-device** with maximal hardware utilization on Qualcomm platforms.

---

## Installation & Quickstart

Please refer to the current project documentation for install steps, environment notes, and code examples:

- TODO: add the GenieX-Bridge Python binding overview link.
- TODO: add the GenieX-Bridge Python binding quickstart link.

The documentation should cover supported Python versions, backend requirements (NPU, GPU, CPU), and ready-to-run examples for LLMs, VLMs, embeddings, and audio.

---

## Links

- TODO: add the GenieX-Bridge Python documentation link.
- TODO: add the current issue tracker link.
- TODO: add the current community channel links.

---
