---
title: 快速开始
description: 几分钟内上手 Nexa AI 的 Android SDK。
---

import Feedback from "/snippets/page-feedback.mdx";

## **安装**
Nexa AI Android SDK 可从 Maven Central 获取，最新版本为 [0.0.24](https://repo1.maven.org/maven2/ai/nexa/core/0.0.24/)。你也可以在 [Maven Repository](https://mvnrepository.com/artifact/ai.nexa/core) 查看（更新速度较 Maven Central 略慢）。
### **添加依赖**
在你的 `app/build.gradle.kts` 中添加以下依赖以导入 NexaSDK：
```kotlin
dependencies {
  implementation("ai.nexa:core:0.0.24")
}
```
<Note>
  默认仓库为 mavenCentral()。你也可以在 settings.gradle.kts 中添加 GitHub 的 raw 仓库：
  ```kotlin
  dependencyResolutionManagement{
      repositories {
          maven {
              url = uri("https://raw.githubusercontent.com/NexaAI/core/main")
          }
      }
  }
  ```
</Note>
## **运行你的第一个模型**
<Steps>
  <Step title="初始化 SDK">
    在你的 Android 应用中初始化 NexaSDK：
    ```kotlin
    NexaSdk.getInstance().init(this)
    ```
  </Step>
  <Step title="下载并加载模型">
    创建 VLM 封装并加载用于 NPU 推理的模型。你可以从 HuggingFace 下载 [OmniNeural-4B 模型](https://huggingface.co/NexaAI/OmniNeural-4B-mobile)，将其放置在你的应用有权限访问的目录，例如：`/data/data/${your-app-packagename}/files/models/OmniNeural-4B`
    ```kotlin
    VlmWrapper.builder()
        .vlmCreateInput(
            VlmCreateInput(
                model_name = "omni-neural",
                model_path = <your-model-file-absolute-path>,
                config = ModelConfig(
                    max_tokens = 2048,
                    enable_thinking = false
                ),
                plugin_id = "npu"
            )
        )
        .build()
        .onSuccess { vlmWrapper = it }
    ```
    <Note>
      应用必须有权限访问模型目录。例如，在示例应用中，model_path 设置为 `/data/data/com.nexa.demo/files/models/OmniNeural-4B/files-1-1.nexa`
    </Note>
  </Step>
  <Step title="生成内容">
    使用已加载的模型生成响应：
    ```kotlin
    vlmWrapper.generateStreamFlow("Who are you?", GenerationConfig()).collect { result ->
        println(result)
    }
    ```
  </Step>
</Steps>
## **支持的模型**
我们为你整理了可用模型合集：
<CardGroup cols={2}>
  <Card title="NPU 模型" icon="microchip" href="https://huggingface.co/collections/NexaAI/qualcomm-npu-mobile">
    针对 Qualcomm NPU 优化的模型
  </Card>
  <Card title="GGUF 模型" icon="file-code">
    任意 GGUF 格式的 LLM 与 VLM 模型
  </Card>
</CardGroup>
## **设备兼容性**
<AccordionGroup>
  <Accordion title="NPU 支持" icon="microchip">
    - Qualcomm Snapdragon 8 Elite
    - Qualcomm Snapdragon 8 Elite Gen 5
  </Accordion>
  <Accordion title="GPU 支持" icon="bolt">
    - Qualcomm Adreno GPU
  </Accordion>
  <Accordion title="CPU 支持" icon="memory">
    - ARM64-v8a
  </Accordion>
</AccordionGroup>
## **后续步骤**
<Card title="API 参考" icon="book" href="/cn/nexa-sdk-android/APIReference">
  查看各模型类型的详细文档与使用方式。
</Card>
<br />
<Feedback/>