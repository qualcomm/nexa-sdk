name: Reusable Python Build

on:
  workflow_call:
    inputs:
      platform:
        description: "Target platform: linux-arm64 or windows-arm64"
        required: true
        type: string
      runner:
        description: "Runner label"
        required: true
        type: string

permissions:
  contents: read

jobs:
  build-python:
    name: build-python
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Placeholder
        run: echo "Python wheel build - not yet implemented"
