name: Test

# Placeholder for the pytest-via-FFI harness (matrix: plugin x platform).

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    name: test
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Placeholder
        run: 'echo "TODO: wire pytest harness."'
